The first queue dashboard deployment is blocked by a migration that loads 4.8 million job records into memory. Stream the records and verify all removals before rewriting collection files.

One partial-rerun clone has a uniquely matching original whose completion timestamp differs by one second. Accept only that narrow match, and shorten collection on the migration run so the full report rebuild and Pages deployment fit the workflow timeout.
